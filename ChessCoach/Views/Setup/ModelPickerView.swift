import SwiftUI

// Phase 4 uses FoundationModels (Apple Intelligence) — no model selection required.
// This view is retained for spec §3 structure compliance and future GGUF fallback (Phase 5+).
struct ModelPickerView: View {
    var body: some View {
        Text("Model picker — not needed with Apple Intelligence")
            .padding()
    }
}
